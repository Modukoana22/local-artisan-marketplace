name: Auth CI Pipeline
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DATABASE_URL: "mysql://root:root@localhost:3307/artisan_marketplace"
  NEXTAUTH_URL: "http://localhost:3000"
  NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}

jobs:
  auth-test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_USER: marketplace_user
          MYSQL_PASSWORD: password
          MYSQL_DATABASE: artisan_marketplace
          MYSQL_ROOT_HOST: '%'
        ports: [3307:3306]
        options: >-
          --health-cmd="mysqladmin ping -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Clear npm cache 
        run: npm cache clean --force
      
      - name: Install dependencies
        run: |
          npm install -g next    # Install Next.js globally
          npm ci
          npm install -g mysql2  # Install MySQL client
      
      - name: Setup Database
        run: |
          npx prisma generate
          npx prisma db push --skip-generate --accept-data-loss
          echo "Database schema synchronized"
      
      - name: Verify Database
        run: |
          # Using native MySQL client instead of prisma db execute
          mysql -h 127.0.0.1 -P 3307 -uroot -proot artisan_marketplace -e "SHOW TABLES;"
          echo "Prisma version: $(npx prisma --version)"
      
      - name: Run Auth Tests
        env:
          DATABASE_URL: "mysql://marketplace_user:password@localhost:3307/artisan_marketplace"
        run: |
          # Create minimal setup file if missing
          if [ ! -f jest.setup.js ]; then
            echo "console.log('Jest setup initialized');" > jest.setup.js
          fi
          npm test -- __tests__/smoke.test.js
      
      - name: Build Sign-in Page
        run: npx prisma generate && next build
<<<<<<< HEAD
        
=======
>>>>>>> f72b321cbe3ea795228328a27f085bb6ce079cc5
